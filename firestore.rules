rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function usuarioAutorizado() {
      return request.auth != null
        && request.auth.uid in [
          "rMURmjHzuVdfaQyeikEAAYdAJxi1",
          "r5yVCCMatXPVsCiiJcMKWM613gq1"
        ];
    }

    // Configuração privada do servidor. Clientes nunca podem ler ou escrever a chave Gemini.
    match /system/{document=**} {
      allow read, write: if false;
    }

    match /users/{uid}/{document=**} {
      allow read, write: if usuarioAutorizado() && request.auth.uid == uid;
    }
  }
}
